apiVersion: image.toolkit.fluxcd.io/v1
kind: ImageRepository
metadata:
    name: ksbh
    namespace: ksbh
spec:
    secretRef:
        name: flux-image-update-registry-auth
    image: nuwa24/ksbh
    interval: 5m
